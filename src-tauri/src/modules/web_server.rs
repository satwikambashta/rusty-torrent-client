// Web server module - currently unused
// This module contains infrastructure for a separate web UI server
// that could serve the web/ directory frontend independently of Tauri.
// Currently, the application uses Tauri's built-in webview for the UI.

// TODO: Implement web server if separate web UI is needed
