# Web Client Configuration

# API Base URL (defaults to /api via Vite proxy during dev)
# VITE_API_BASE=http://localhost:8080/api

# Refresh interval in milliseconds (default: 5000)
# VITE_REFRESH_INTERVAL=5000
